# Continuation handoff — 2026-09-14 (stop-state)

## Takeover state

The previous `geistos` work had already landed the Quickshell bar/cards, launcher over desktop entries and command actions, session menu with suspend/reboot/poweroff, wallpaper/theme services, automatic palette plumbing, theme-client synchronization, calendar/reminder cards, local AI panel, PostgreSQL resolver, and the suite projection test. The package repository is no longer clean: inspect its current status before touching it. Its older `docs/HANDOFF.md` remains the authoritative history for the suite migration work.

The requested feature list is substantially broader than the current implementation. There is no provider-based Raycast-style launcher, shared math engine, dedicated calculator, graphing UI, notification-sound mapping, or full system-wide light/dark propagation in the repository yet. These remain planned rather than silently treated as complete. The existing bar is top-anchored; bottom-bar and pinned/HUD modes remain open design/implementation work.

Related checkout state at takeover was not uniformly clean: `mg-coreforge/bootcamp` already had a modified routing note and an untracked switching note. Those files were preserved and not committed or rewritten.

The launcher now has a shared provider/result seam for applications and
command actions. It preserves the existing search ranking, keyboard selection,
command catalog, and activation behavior while giving future calculator,
conversion, dictionary, and thesaurus providers a stable result shape.

## Completed in this pass

- Added the shared launcher provider/result seam for applications and command actions.
- Added symmetric `SUPER + SHIFT + arrow` window-movement bindings and descriptions.
- Added `SUPER + CTRL + SHIFT + W` wallpaper-service recovery through a Quickshell IPC method.
- Made wallpaper application update a stable Hyprlock background symlink through the existing wallpaper event path.
- Added `docs/KEYBINDINGS.md` and linked it from the repository README.
- Added a public routing-technologies page from the networking implementation notes, adapted to the website's authoring contract.
- Added routing glossary terms without duplicating existing entries: administrative distance, autonomous systems/ASNs, BGP, dynamic/static routing, EIGRP, FHRP, NAT/PAT, OSPF, and VIP.
- Added a Geistos engineering-process blog draft grounded in the current repository state.
- Updated the GitHub profile README, public-face guidance, and LinkedIn draft with a conservative Geistos description and current systems/Linux/networking trajectory.

## Verification

- `geistos`: `luac -p config/hypr/keybindings.lua` passed.
- Quickshell loaded the edited configuration successfully under `qs -p ... --no-duplicate`; the process was then stopped by the timeout. The only warning was an already-running notification server.
- `mg-server`: `cargo test --quiet` passed 120 unit/bin tests and the content suite after the routing page was brought into the site's required sections.
- `git diff --check` passed for geistos, mg-server, and machinageist.

## Stop-state and consolidation boundary

- The verified production source is `/home/mgeist/dotfiles`, branch
  `quickshell-shell`, clean at `31dd1c2 launcher: use Rust calculator provider`.
- The dedicated calculator and launcher calculator use the Rust `mg-calcr` CLI
  in `/home/mgeist/geistos/mg-suite/mg-calcr`; that repository is locally
  verified but remains separate from the package repository.
- No package merge, production relink, or cleanup was performed after this
  state was discovered. Do not claim that `~/geistos` and `~/dotfiles` are
  synchronized.
- `~/geistos` has pre-existing dirty tracked files and untracked math/provider
  files. Preserve them. Compare them file by file against dotfiles before any
  merge; never use broad copy, reset, checkout, or clean operations.
- The next consolidation session should inspect `~/.config/quickshell/mgeist`,
  `~/.config/hypr`, service unit paths, and the installer before deciding which
  paths can safely point at dotfiles. Verify symlink targets and restart/read
  back only after a deliberate, scoped change.
- Verified production topology: `~/.config/quickshell` already points to
  `/home/mgeist/dotfiles/config/quickshell`; `mgeist` is a normal child
  directory inside that linked parent. Do not replace the child with another
  link, because that creates a self-referential loop and prevents Quickshell
  from finding its configuration. The attempted child-link was rolled back;
  the service was restored and verified active with `Configuration Loaded`.
- Measured divergence before any package merge: 106 of 181 Quickshell files
  differ between `~/geistos/config/quickshell/mgeist` and
  `~/dotfiles/config/quickshell/mgeist`. The package also has its own dirty
  launcher/shell edits and untracked provider/math implementation. A bulk
  mirror is therefore unsafe; the next session needs an explicit file-level
  ownership plan before merging.

## Package reconciliation slice completed

- Ported the verified Rust-backed launcher calculator boundary into the package
  without removing the package's existing Python/QML math files.
- Added the package copies of `LauncherProviders.js`, `MathEngine.js`, and
  `Services/AppLaunch.qml`; updated `Launcher/Launcher.qml` to use the
  debounced `mg-calcr evaluate --json` path and unit conversion.
- Ported the optional lexical lookup closure as `Panels/LookupPanel.qml`, its
  `lookup` shell IPC handler, and launcher activation. Network/API failure is
  still reported as a controlled optional-provider error.
- Ported the shared popup focus/inspection primitive: `Services/PanelState.qml`
  and the compatible `Widgets/PopupPanel.qml` behavior. Existing package panels
  still load successfully with the new activation/release state.
- External preservation snapshot for the pre-existing package working tree:
  `/home/mgeist/.geistos-reconcile-backups/20260914_223343`.
- This slice is verified, but the broader package is not synchronized with
  dotfiles; do not describe it as a complete mirror.

## Remaining work

1. Reconcile remaining dedicated-panel dependencies and package theme/bar
   differences file by file, preserving package-local work and testing each
   slice.
2. Keep network lookup optional when unavailable; add offline data only if a
   real local source is deliberately selected.
3. Add AI-output copy tests and verify streamed/completed-output behavior through the existing clipboard path.
4. Finish wallpaper/theme event coverage, palette contrast tests, theme-derived Hyprland/bar borders, and system preference propagation.
5. Decide and implement bar geometry/position configuration, module touching, pinned widgets, and glow quality tiers.
6. Add notification sound policy with DND integration.
7. Complete packaging/installer work and clean-machine verification.
8. Commit the pre-existing bootcamp routing-note changes separately if desired; do not fold them into the website commit.

## Next-agent prompt

Inspect reality first. The verified production Quickshell source is `/home/mgeist/dotfiles` on `quickshell-shell`; the package tree `/home/mgeist/geistos` is divergent and dirty, with 106 of 181 Quickshell files differing and its own untracked provider/math implementation. Do not redo the published calculator/provider work or bulk-mirror either tree. Production already uses the safe parent symlink `~/.config/quickshell -> /home/mgeist/dotfiles/config/quickshell`; never symlink its `mgeist` child again. Establish explicit file-level ownership before merging package work, preserve all dirty files, and ask for the architecture choice if needed. Continue only after consolidation is scoped, then implement the highest-value remaining dependency-ready slice, verify it, record it, and keep the loop non-destructive.
